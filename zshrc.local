bindkey "^B" backward-char
bindkey "^F" forward-char
bindkey "^K" kill-line
bindkey "^N" history-search-forward

export GOPATH=~/Validic
export PATH=$GOPATH/bin:$PATH

eval "$(rbenv init - --no-rehash)"
